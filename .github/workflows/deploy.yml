# Deploy Pipeline for Portfolio - FIXED
# Location: .github/workflows/deploy.yml
# Version: Post-monorepo split with fixes

name: Deploy to DigitalOcean

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    
    steps:
      - name: Setup deployment info
        run: |
          echo "🚀 Starting portfolio deployment"
          echo "Commit: ${{ github.event.workflow_run.head_sha }}"
          echo "Triggered by: ${{ github.event.workflow_run.name }}"
          echo "Run ID: ${{ github.event.workflow_run.id }}"
      
      - name: Download deployment artifact
        uses: dawidd6/action-download-artifact@v6
        with:
          run_id: ${{ github.event.workflow_run.id }}
          name: deployment-package-${{ github.event.workflow_run.head_sha }}
          path: deployment-files
      
      # Verify downloaded artifact
      - name: Verify downloaded artifact
        run: |
          echo "📦 Verifying downloaded artifact..."
          
          echo "📁 Downloaded contents:"
          ls -la deployment-files/
          
          # Check for .next directory
          if [ ! -d "deployment-files/.next" ]; then
            echo "❌ ERROR: .next directory not found in artifact!"
            echo "Current structure:"
            find deployment-files -type d -maxdepth 2
            exit 1
          fi
          
          echo "✅ Found .next directory"
          echo "📊 .next size: $(du -sh deployment-files/.next | cut -f1)"
          
          if [ ! -f "deployment-files/package.json" ]; then
            echo "❌ ERROR: package.json not found!"
            exit 1
          fi
          
          echo "✅ All required files present"
      
      # Prepare deployment on server first
      - name: Prepare deployment directory
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            
            echo "🔧 Preparing deployment..."
            
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH_PORTFOLIO }}"
            TEMP_PATH="$DEPLOY_PATH-temp-$(date +%s)"
            
            echo "📁 Creating temporary deployment directory..."
            mkdir -p "$TEMP_PATH"
            
            # Save the temp path for next steps
            echo "$TEMP_PATH" > /tmp/portfolio-deploy-path.txt
            
            # Backup current .env if it exists
            if [ -f "$DEPLOY_PATH/.env" ]; then
              echo "💾 Backing up .env file..."
              cp "$DEPLOY_PATH/.env" /tmp/portfolio-env-backup
            fi
            
            # Backup user-uploaded images if they exist
            if [ -d "$DEPLOY_PATH/public/images/projects" ]; then
              echo "📸 Backing up user-uploaded images..."
              cp -r "$DEPLOY_PATH/public/images/projects" /tmp/portfolio-images-backup || true
            fi
            
            echo "✅ Preparation complete"
      
      # Transfer files to server
      - name: Transfer files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "deployment-files/*"
          # We'll move it to the right place in the next step
          target: "/tmp/portfolio-deployment"
          strip_components: 1
          rm: true
      
      # Complete deployment and restart
      - name: Complete deployment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            
            echo "🔧 Setting up Node environment..."
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH_PORTFOLIO }}"
            TEMP_PATH=$(cat /tmp/portfolio-deploy-path.txt)
            
            # Move files from /tmp to actual deployment directory
            echo "📦 Moving files to deployment directory..."
            mv /tmp/portfolio-deployment/deployment-files/* "$TEMP_PATH/"
            
            # Restore .env file
            if [ -f /tmp/portfolio-env-backup ]; then
              echo "♻️ Restoring .env file..."
              cp /tmp/portfolio-env-backup "$TEMP_PATH/.env"
            fi
            
            # Restore user-uploaded images
            if [ -d /tmp/portfolio-images-backup ]; then
              echo "♻️ Restoring user-uploaded images..."
              mkdir -p "$TEMP_PATH/public/images"
              cp -r /tmp/portfolio-images-backup "$TEMP_PATH/public/images/projects"
            fi
            
            # Swap directories
            echo "🔄 Swapping deployment directories..."
            if [ -d "$DEPLOY_PATH" ]; then
              mv "$DEPLOY_PATH" "$DEPLOY_PATH-old-$(date +%s)"
            fi
            mv "$TEMP_PATH" "$DEPLOY_PATH"
            
            # Install production dependencies
            cd "$DEPLOY_PATH"
            echo "📦 Installing production dependencies..."
            npm ci --omit=dev || npm install --production
            
            # Restart portfolio application
            echo "🔄 Restarting portfolio application..."
            APP="${{ secrets.PM2_APP_NAME_PORTFOLIO }}"
            
            if pm2 describe "$APP" >/dev/null 2>&1; then
              pm2 restart "$APP" --update-env
              echo "✅ Portfolio restarted: $APP"
            else
              pm2 start npm --name "$APP" -- start
              echo "✅ Portfolio started: $APP"
            fi
            
            pm2 save || true
            
            # Show application status
            echo "📊 Application status:"
            pm2 show "$APP" || true
            
            # Cleanup
            echo "🧹 Cleaning up..."
            rm -rf /tmp/portfolio-deployment
            rm -f /tmp/portfolio-deploy-path.txt
            rm -f /tmp/portfolio-env-backup
            rm -rf /tmp/portfolio-images-backup
            ls -dt $DEPLOY_PATH-old-* 2>/dev/null | tail -n +3 | xargs rm -rf || true
            
            echo "✅ Portfolio deployment completed!"
      
      # Final success notification
      - name: Deployment summary
        run: |
          echo "🎉 PORTFOLIO DEPLOYMENT SUCCESSFUL!"
          echo "================================"
          echo "Commit: ${{ github.event.workflow_run.head_sha }}"
          echo "Portfolio has been deployed to production."
          echo ""
          echo "Note: Telegram bot is now deployed separately from:"
          echo "https://github.com/undevy-org/telegram-bot"
  
  # Handle failed CI
  failure-notification:
    name: Handle CI Failure
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    
    steps:
      - name: Report CI failure
        run: |
          echo "❌ CI Pipeline failed!"
          echo "Commit: ${{ github.event.workflow_run.head_sha }}"
          echo "Deployment cancelled due to failed tests or build."
          echo "Check the failed run: ${{ github.event.workflow_run.html_url }}"
          exit 1