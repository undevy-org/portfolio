# ================================
# PORTFOLIO CONFIGURATION TEMPLATE
# ================================
# Copy this file to .env.local for development
# or .env for production and fill in your values

# === CORE APPLICATION ===
NODE_ENV=development
PORT=3000

# Local development IP (your machine in LAN, used for testing from phone/tablet)
LOCAL_IP=192.168.0.100

# === CONTENT MANAGEMENT ===
# Path to your main content.json file
CONTENT_FILE_PATH=/path/to/content.json
# Path to backup directory
BACKUP_DIR=/path/to/content-backups
# Demo mode: when true, uses example data for non-authenticated users
ENABLE_DEMO_MODE=false

# === ADMIN API ===
# Secret token for admin API authentication
ADMIN_TOKEN=your-secret-admin-token-here

# === MASTER CODE ACCESS ===
# Master code for administrative access to all system codes
MASTER_CODE=your-master-code-here

# === ANALYTICS (Matomo) ===
# Your Matomo analytics URL (without trailing slash)
NEXT_PUBLIC_MATOMO_URL=https://analytics.yourdomain.com
# Matomo site ID
NEXT_PUBLIC_MATOMO_SITE_ID=1
# Matomo API token for bot integration
MATOMO_TOKEN=your-matomo-api-token-here

# === BROWSER TAB ===
# The public-facing name of the portfolio, displayed in the browser tab
PORTFOLIO_TITLE=Interactive Portfolio

# === WEB3 INTEGRATION ===
# Reown (WalletConnect) project ID
NEXT_PUBLIC_REOWN_PROJECT_ID=your-project-id-here

# === WEB3 AUTHENTICATION ===
NEXT_PUBLIC_WEB3_SHARED_ACCESS_CODE=your_default_web3_access_code_here

# === DOMAIN CONFIGURATION ===
# Instead of JSON, we use a separate config file
# This points to the domain configuration file
DOMAIN_CONFIG_PATH=./config/domains.json

# === DEFAULT CONTACT INFO ===
# Used as fallback when domain-specific config is not available
DEFAULT_CONTACT_EMAIL=contact@example.com
DEFAULT_CONTACT_TELEGRAM=@example
DEFAULT_CONTACT_WEBSITE=https://example.com
DEFAULT_PORTFOLIO_TITLE=Terminal Portfolio

# === DEMO CONFIGURATION ===
# Used for demo mode settings
DEMO_CONTENT_FILE_PATH=./config/demo-content.json